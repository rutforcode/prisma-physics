/**
 * Seed credentials for the demo admin account. The `admin.ensureAdmin`
 * action creates this account on first use (idempotent — it never
 * overwrites an existing admin). Demo-only: change the password after
 * first sign-in for anything real.
 */
export const ADMIN_EMAIL = "admin@prism.dev";
export const ADMIN_PASSWORD = "Admin@1234";
